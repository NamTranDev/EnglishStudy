import 'package:flutter/cupertino.dart';

class ListenerTab extends StatelessWidget {
  const ListenerTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Listen Tab'),
      ),
    );
  }
}